load('ext://deployment', 'deployment_create')

deployment_create('postgres', 'postgres:alpine', ports=['5432:5432'], env=read_yaml('./dev.env'),
    startupProbe={'exec':{'command': 'netstat -ln | grep 5432'.split(' ')}, 'failureThreshold': 10})
#    startupProbe={'exec':{'command': ['psql', '-U', 'postgres', '-c', '"SELECT 1"']}, 'failureThreshold': 10})

k8s_resource('postgres', port_forwards=['5432'], labels='dev')